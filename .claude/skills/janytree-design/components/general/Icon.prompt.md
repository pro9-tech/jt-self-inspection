모든 글리프에 `Icon`을 사용하세요. 아이콘 체계는 Google Material Symbols Rounded 하나뿐입니다 — 다른 아이콘 세트, 이모지, 직접 그린 SVG를 섞지 마세요.

```jsx
<Icon name="science" size={20} />
<Icon name="verified" filled />
```

크기: 20(조밀), 24(기본), 40(빈 화면). 색상은 `currentColor`로 상속되며, `filled`는 선택 상태에만 씁니다.
