/** Date field for issuance dates and validity periods; same geometry as Input. */
export interface DatePickerProps { value?: string; onChange?: (e: React.ChangeEvent<HTMLInputElement>) => void; disabled?: boolean; style?: React.CSSProperties }
export declare function DatePicker(props: DatePickerProps): JSX.Element;
