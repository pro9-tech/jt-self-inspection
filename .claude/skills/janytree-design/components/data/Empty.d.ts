/** Empty state — 40px icon, one line of guidance, exactly one primary action. */
export interface EmptyProps { icon?: string; title?: string; description?: string; action?: React.ReactNode; style?: React.CSSProperties }
export declare function Empty(props: EmptyProps): JSX.Element;
